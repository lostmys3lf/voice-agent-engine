"""Scenario configs — one module per use case, each exporting a SCENARIO dict.

These files are DATA consumed by the engine (see engine/scenario.py for the
schema). Adding a new use case means adding one file here, nothing else.
"""
